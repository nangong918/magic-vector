<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AppDemo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/s1_400</item>
        <item name="colorPrimaryVariant">@color/s1_550</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/a1_200</item>
        <item name="colorSecondaryVariant">@color/a1_600</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Customize your theme here. -->
        <!-- 设置窗口背景颜色 -->
        <item name="android:windowBackground">@color/s1_0</item>
        <!-- 沉浸式 -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Android 6.0以上 状态栏字色和图标为浅黑色-->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="MyNavigationViewStyle" parent="Widget.Design.NavigationView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_gravity">start</item>
        <item name="android:background">@drawable/shape_navi_view</item>
        <item name="itemTextColor">@color/s1_800</item> <!-- 设置字体颜色为黑色 -->
        <item name="itemIconTint">@color/s1_800</item> <!-- 设置图标颜色为黑色 -->
    </style>
</resources>